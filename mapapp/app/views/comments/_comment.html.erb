

<%= comment.name %>
<br />
<%= comment.description %>
<p>
<%= time_ago_in_words(comment.created_at) %> ago
</p>
<br />
<%= link_to 'Delete', [comment.map, comment], method: :delete, data: { confirm: 'Are you sure?' } %>